# TSV terminal tabs preserve explicit empty final fields and are data, not style.
*.tsv whitespace=-trailing-space

# Frozen Pilot contracts/reports use Markdown two-space hard breaks. Their bytes
# are also bound by execution hashes, so whitespace cleanup would falsify provenance.
pilots/hcc-sc-spatial-npj-2026/**/*.md whitespace=-trailing-space
