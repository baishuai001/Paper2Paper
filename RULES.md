# Paper2Paper 绑定规则

1. **终点是论文。** 新增工作必须直接服务于路线选择、数据、代码、Figure、claim、稿件或审查。
2. **模仿是合法起点。** marker、基因集、细胞、癌种、泛癌、signature和组合替换均可接受。
3. **不设创新性准入门槛。** 但必须检索最近发表，实质重复路线必须停止。
4. **先生成组合，再选择。** 不得只给一个AI偏好的方向；遗漏的常见路线必须说明原因。
5. **可执行性必须有字段级证据。** “有accession”不等于数据可用；每个必需字段要落到实际资源，同一队列的每个分析角色都要登记。
6. **代码可以重建。** 重建模块必须记录donor、版本、输入输出、模型合同、目标环境与测试，达到真实数据smoke-test标准后才能支撑路线。
7. **统计单位服从claim。** 细胞、spot、切片和重复样本不能冒充独立患者。
8. **失败和结果都可以改变计划。** 安装、解析和运行失败以及阴性结果必须保留；refine、reroute和stop必须有记录，不得悄悄覆盖旧结果。
9. **AI负责执行，用户负责批准。** 路线选择、中心主张和高影响变更由用户决定。
10. **最低充分即停止平台建设。** 一条路线具备真实数据、可靠代码、Figure闭环和可写稿件后，转入研究执行。
11. **跑通不等于可投稿。** 训练复现和辅助分析可以执行并保留，但只有明确标记为稿件候选、且非实质重复的路线才能进入正式选择。
12. **执行状态与稿件资格分开。** `execution_ready`只表示该路线的已登记数据、代码和最低产物通过；`manuscript_eligible`还要求它是稿件候选且通过发表重合审查。
13. **实例不能冒充通用基准。** 一个pilot只证明它实际运行过的模态、模块和合同；其数据、公式、预期结果和科学判断不得成为另一篇论文的默认值。
14. **校验层级必须说清。** 结构校验、单元测试、真实数据运行和科学审查是四类不同证据，不得统一表述为“验证通过”。
