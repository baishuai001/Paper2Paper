# Paper2Paper 项目纪律

本文件是长期约束。代码、文档、判断和更新都必须服务于：帮助初学者从真实锚点文献出发，可靠地形成一篇目标论文。

## 不可改变的产品边界

1. **面向初学者。** AI 负责调查、核验、比较、实现和解释；用户审查高影响决定，不替 AI 完成原始分析工作。
2. **简单模仿路线可以接受并应被认真考虑。** 不因改变少、方法常见或故事简单而淘汰路线。
3. **数据必须能够取得。** 正式执行前，必需文件要真实下载并解析到字段、ID、统计单位和队列用途。
4. **代码需要可靠、稳定，缺失时要从同类文献补足。** 锚点代码不合格时，应检索同类论文、方法论文和官方实现，记录 donor、版本、许可和适配，并以测试和真实输入运行验收。
5. **避免实质重复发表。** 重复判断针对具体研究组合，而非整个方法、癌种、细胞、signature 或其他模仿类别。
6. **最终目的是产出论文。** workflow 的任何建设都不得无故延迟已经可以推进的数据、Figure、分析和写作。
7. **不能重新引入 PaperRoute 的创新性门槛。** 不使用创新等级、改动幅度、想象中的期刊档次或隐藏综合分数决定路线资格。

## 锚点文献与路线

8. 先完整还原锚点文献自己的问题—证据—分析—Figure 框架，再生成目标论文候选；不得用固定替换菜单代替具体审计。
9. 每项硬伤都必须同时给出受影响 claim、仍可利用的框架、目标论文修复方案、验证条件和剩余风险。指出问题不能自动触发放弃锚点或另造无关方向。
10. 近邻论文同时承担两种角色：检查具体实质重复；提供数据、代码、方法和 Figure 设计。停止其已发表组合，不得连带丢弃其 donor 价值。
11. AI 在最低可比预检后给出明确首选和备选，并说明依据、反对理由、不确定性及什么新证据会改变判断；不得把未核验候选表交给用户猜选。

## 数据、代码与证据

12. accession、网页、README、安装成功或“图看起来合理”都不等于证据完成。数据需真实解析，代码需在目标环境运行，Figure 需有 source table 和验收条件。
13. 患者、样本、细胞、spot 和切片不能混作同一统计单位；跨来源连接需验证 ID 唯一性、集合覆盖和未匹配数；参与筛选或调参的队列不能称为独立外部验证。
14. 失败、阴性结果、缺失数据、无法复现部分和 claim ceiling 必须保留。不能通过改标签或覆盖旧运行制造“成功”。
15. 结构校验、单元测试、真实数据运行、科学审查和用户批准是不同证据层，任何一层都不能冒充另一层。

## 主循环与次循环

16. 主循环始终是：锚点审计 → 候选路线 → 可得性核验 → 最小真实运行 → 用户批准 → 正式论文。
17. 次循环始终是：真实文献暴露 workflow 问题 → 先修复当前实例 → 必要时修改 Paper2Paper → 用同一实例复验 → 回到主循环。
18. 只有能减少真实论文错误、时间或审查负担的发现才进入 Paper2Paper。不得为假想复用建立中央能力、模块成熟度、跨项目发布或其他重型基础设施。
19. 必要测试只保护会造成错误数据、错误结论、假运行、假晋升或不可复现的行为；测试数量、schema 复杂度和平台功能不是成果。

## Workspace 与决定

20. Pilot 用于真实锚点审计和最小试验；正式目标论文只能在用户批准后建立为独立 `manuscript-project`。Pilot 结果不能直接冒充正式分析结果。
21. 用户必须批准正式路线、中心 claim、重大 reroute 和最终发布。AI 必须先完成证据工作并给出判断，不能以“需要用户决定”为名提前停止调查。
22. 已经具备真实数据、可靠代码、Figure 闭环、合理 claim 和非重复依据的获批路线，应立即转入论文生产；除非出现新的真实阻断，不再扩建 workflow。

## 安全与版本纪律

23. 不提交凭据、患者可识别信息、受控数据、未经许可的材料或不宜再分发的逐患者派生表。
24. 输入、代码、环境、命令、日志、产物和高影响决定要可追溯。Git 保留历史，但历史存在不等于当前内容仍应执行；与本纪律冲突的旧产品逻辑必须删除。
