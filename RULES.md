# Paper2Paper 绑定规则

以下规则约束 Paper2Paper 的代码、文档、Pilot、正式论文项目、判断和更新。它们的共同目的，是帮助初学者从真实文献出发，可靠地完成一篇论文。

## 七项不可变要求

1. **面向初学者。** AI 承担信息搜集、证据核对、路线生成与比较、代码构建、运行、风险判断和明确推荐；表达必须让初学者能够理解和复核。详细表格用于追溯，不得被当作要求用户自行完成的分析作业。
2. **简单模仿路线可以接受并应被认真考虑。** 不得因改变轴少、方法常见或叙事简单而降低路线资格；必须审查具体路线，而不是预先淘汰某类模仿方式。
3. **数据必须能够取得。** 候选路线进入执行前，必需数据必须能够真实下载、解析，并包含支持相应统计单位、比较、结局、Figure 和 claim 的字段；仅有 accession、链接或论文声明不算可得。
4. **代码需要可靠、稳定，缺失时要从同类文献补足。** 锚点代码缺失或不合格时，AI 必须检索同类论文、方法论文和官方实现，组合可靠 donor 并补齐适配、合同、测试和真实输入运行；不能凭记忆重写，也不能因锚点代码差而放弃其分析框架。
5. **避免实质重复发表。** 最近发表检索必须判断一条具体路线是否在研究问题、数据、对象、关系、结局和证据链上与已发表工作实质重合。停止具体重复路线，不得把简单 signature、泛癌、替换 marker/细胞/癌种等类别整体判死。
6. **最终目的是产出论文。** 主循环是从锚点文献形成目标论文；Paper2Paper 的架构和产品完善只是解决真实论文阻断问题的次循环，不能反客为主。
7. **不能重新引入 PaperRoute 的创新性门槛。** Paper2Paper 不设置抽象创新性准入、创新程度排名或期刊档次导向，也不得用“综合价值”“变化幅度”“迁移价值”“情境边界”等替代词暗中恢复同一门槛。路线判断只能针对其具体科学设计、数据、代码、Figure、负担和实质重复证据。

这七项要求优先约束后续所有规则、schema、脚本、Pilot 和 AI 输出；发生冲突时必须按这七项修正。AI 必须在可比的最低证据基础上给出首选、备选、反对理由和不确定性，用户只承担正式路线、中心主张、重大变更和发布等高影响批准，不能把本应由 AI 完成的搜集、分析和初步判断全部转交给用户。

## 一、目的和决策权

1. **终点是从锚点文献产出目标论文。** 新增工作必须直接服务于锚点审计、路线选择、数据、代码、Figure、claim、稿件或对这些内容的审查；架构、schema 和自动化只是手段。
2. **主循环优先于产品循环。** 真实文献分析和正式论文产出是主任务；Paper2Paper 自身完善只能作为解决真实阻断问题的次循环。
3. **模仿是开放式框架迁移。** 候选改变轴必须从锚点文献自己的问题、对象、关系、尺度、数据角色、证据顺序、方法模块和 Figure 结构中生成。marker、基因集、细胞、癌种、泛癌和 signature 只是示例；不得把任何预设列举当作封闭分类、必选菜单或思考边界，也不因改动简单而排除。
4. **不设抽象创新性门槛，但禁止实质重复。** 必须检索最近发表并说明与最接近论文的区别；只有具体路线被证明确属实质重复时才停止或改作训练复现，不能由路线类别代替该判断。
5. **AI 负责执行和提出有依据的判断，用户负责高影响批准。** AI 必须先完成证据搜集、比较和明确推荐，不得只交付待填表格或无结论的候选清单；正式路线、中心 claim、claim ceiling、结果后的 reroute、模块/核心重大晋升和最终发布都必须留下用户决定。

## 二、四层边界

6. **core、module、pilot 和 manuscript-project 必须分层。**
   - core 只保存跨论文通用的规则、schema 和校验器；
   - module 保存可复用分析能力、合同、实现和测试；
   - pilot 保存一篇真实锚点文献的审计、路线和最小真实运行；
   - manuscript-project 保存用户批准后的正式论文路线、完整分析和稿件。
7. **下游事实不得成为上游默认值。** 某个 Pilot 的疾病、数据、公式、cutoff、预期结果和科学判断不得进入 core，也不得成为另一篇论文的默认配置。
8. **Pilot 不是正式论文项目。** Pilot 可以产生“可晋升候选”，但只有用户批准并建立 manuscript-project 后，才进入冻结分析和完整稿件生产。
9. **module 不是代码片段仓库。** 只有具有明确能力 ID、输入输出合同、科学不变量、许可、版本和测试范围的实现才可登记为模块。

## 三、路线、数据、代码和结果

10. **先保存并拆解框架，再生成组合。** 先还原锚点文献的中心问题与关系、证据顺序、分析模块、数据/队列角色和 Figure 叙事，再对实际存在的要素逐项判断保留、修复、替换、扩展或删除，由此生成目标论文的候选组合。路线覆盖是否充分按该锚点的框架判断，不按通用替换清单计数。
11. **按成本漏斗推进。** 路线依次完成方向审计、可得性预检、最小真实运行和 Figure 闭环。不能要求所有候选一开始就完成全部高成本分析。
12. **可执行性必须有字段级证据。** “有 accession”不等于数据可用；每个必需字段必须落到真实资源，跨来源 ID 和同一队列的分析用途必须登记。
13. **代码可以重建，但不能凭记忆重写。** 重建必须记录 donor、版本、许可、输入输出、模型合同、目标环境和测试，并在真实输入 smoke test 通过后才支持路线。
14. **统计单位服从 claim。** 细胞、spot、切片和重复样本不能冒充独立患者；发现和验证角色必须避免患者重叠和分析泄漏。
15. **Figure 必须服从证据链。** 每幅主图都要映射数据需求、代码模块、source table、统计单位、验收条件和可支持的 claim。
16. **失败和阴性结果必须保留。** 下载、安装、解析、连接、统计和绘图失败，以及削弱或推翻假设的结果，都必须写入运行或结果记录；修复后新增 run，不能覆盖旧证据。`unit`、合成集成和环境 smoke run 必须与 `real_data` run 分开，不能用测试通过冒充真实科学执行。
17. **执行完成不等于可以写成新论文。** 训练复现和辅助分析可以真实跑通，但不能仅凭运行成功成为正式论文路线。

## 四、真实文献到产品规则的晋升

18. **指出硬伤必须同时设计修复。** 每个 finding 先分类为锚点缺陷、Pilot 实现缺陷、同类模块缺陷、core 通用缺陷或科学结果；锚点硬伤还必须记录“证据 → 受影响的 Figure/claim → 仍可利用的框架 → 目标论文中的修复 → 验证/推翻条件 → 剩余风险与 claim ceiling”。指出问题不是目的，不得把科学阴性结果误报为 workflow 故障，也不得用“论文有硬伤”代替修复判断。
19. **修复和利用优先于抛弃框架。** 当前 Pilot 的问题必须先有局部修复、等价数据/代码/方法替代、主张降级或验证设计改造及其证据；不能用“以后加入核心”代替修复当前论文分析。只有合理修复全部失败且中心框架无法支持可证伪的目标论文时，才允许整体停止或换方向，并仍需登记可保留的局部模块。
20. **普通规则至少需要第二个独立案例。** 一个 promotion 从 `observed` 到 `provisional` 可以基于来源 Pilot 和合格测试；只有在不同 Pilot、锚点和数据集的兼容真实案例中成功迁移，才可标为 `confirmed`。Module 同样只有完成独立迁移后才能成为 `transfer_verified`。
21. **高风险保护可以先强制、后验证范围。** 数据泄漏、患者身份错配、隐私、许可证和输入完整性等风险第一次出现即可登记为 `risk_class=safety`、`maturity=provisional` 并临时强制，但必须记录其证据边界，不能声称已经 `confirmed`。
22. **实现通过与适用范围分开。** Module 的 `unit_verified/reference_verified/transfer_verified` 记录实现经过哪类案例；promotion 的 `observed/provisional/confirmed` 记录规则的晋升证据。二者不得合并成“核心已验证”。
23. **废弃规则也要保留历史。** 过度限制、被替代或适用范围判断错误的规则标为 `deprecated`，记录原因和受影响项目，不能悄悄删除。

## 五、能力矩阵、复用和防循环确认

24. **使用能力矩阵，不设单一全局基准。** 每个案例只证明它实际运行过的模态、统计单位、模块和合同；能力矩阵分别记录参考、迁移和失败案例。
25. **跨论文复用必须重新检查兼容性。** 相同方法名称不等于相同输入、统计单位、预处理、结局和 claim；不兼容时必须适配、降级或拒绝复用。
26. **禁止循环确认。**
   - 参与特征筛选、调参或 cutoff 选择的数据不能再被称为独立验证；
   - 模块不能只用自身刚生成的输出证明自身正确，必须有冻结 fixture、科学不变量、独立实现/文献依据或真实参考结果；
   - 来源 Pilot 只能提出并初步测试通用规则，不能单独证明该规则跨 Pilot 通用；
   - 用于生成候选方向的同一结局证据不能未经隔离又承担最终验证。
27. **数据和代码 donor 的来源必须可追溯。** 科学思路来源、代码实现来源、数据来源和 Paper2Paper 的适配/修复必须分别记录，不能笼统写成“参考某论文”。

## 六、回归、PR 和停止条件

28. **回归按影响范围选择。** core 变更重跑受影响能力，module 变更重跑其参考和迁移案例，Pilot 变更只重跑该 Pilot，manuscript-project 变更重跑冻结的正式分析；未受影响的项目不为形式上的全量测试消耗时间。
29. **每次变更都要声明影响。** PR 必须列出目标层、受影响的 capability/module/Pilot/稿件、实际运行的测试以及未被验证的范围。
30. **一个 PR 只处理一个层次。** Pilot 发现、module 提取、core 规则和正式稿件变更分别提交；跨层工作按有依赖关系的多个 PR 顺序推进，不能在一个 PR 中同时改变科学结果和通用规则。
31. **结构校验、单元测试、真实数据运行和科学审查必须分别报告。** 任一层通过都不能替代其他层。
32. **最低充分即停止平台建设。** 当一条用户批准的正式路线已经具备真实数据、可靠代码、Figure 闭环、合理论文主张和可审查稿件时，停止为了通用化而继续扩建；只有新的真实阻断问题才允许恢复产品工作。
33. **没有可验证价值的产品想法进入待办而非当前实现。** 不能因为“以后可能有用”而增加 schema、模块或自动化。

具体执行方法见：

- [架构说明](docs/architecture.md)
- [真实文献学习循环](docs/learning-loop.md)
- [模块复用](docs/module-reuse.md)
- [项目治理](docs/project-governance.md)
