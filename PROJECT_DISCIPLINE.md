# Paper2Paper 项目纪律

本文档是 Paper2Paper 的最高级产品约束。代码、schema、文档、路线推荐、数据整理、
结果解释、反馈和版本更新都不得与本文档冲突。

## 1. 唯一首要目标

Paper2Paper 的首要目标，是帮助初学者以一篇锚点论文为模板，在科学上成立、数据可得、
代码可靠、逻辑可审查的前提下，产出一篇不构成实质性重复发表的论文。

workflow 是服务稿件的工具，不是研究对象。达到预定稿件闭环后，应停止平台优化并转入
分析、写作和投稿准备。

## 2. 模仿是默认起点

模仿不等于逐字、逐图或逐结论复制。它可以保留锚点论文的问题结构、分析顺序、图件语法、
方法模块和叙事骨架，再对一个或多个对象作明确替换。

以下路线默认合法，并应在 G0 主动生成，而不是因“改动不够大”被排除：

- `faithful_reproduction`；
- `marker_substitution`；
- `gene_set_substitution`；
- `cell_type_substitution`；
- `cancer_type_substitution`；
- `pan_cancer_extension`；
- `signature_construction`；
- `combined_substitution`；
- 有明确范围的 `custom` 路线。

每个非原样复现路线仍须记录替换内容、理由、所需证据、代码或方法 donor、风险和停止条件。

## 3. 不设创新性门槛

Paper2Paper 不要求路线证明“创新性”，不保存 `novelty` 评分，不因单轴替换、多轴替换、
修复锚点硬伤或采用成熟分析模板而自动降级，也不把“创新程度”伪装成科学价值、情境边界、
迁移性或其他替代指标重新引入排序。

这不意味着允许重复发表。创新性评价与发表重合检查必须分开：前者不是本 workflow 的
准入条件；后者是提交论文前必须完成的合规与研究定位检查。

## 4. 科学性仍是硬约束

简单替换可以接受，但研究设计不能因此失效。每条稿件路线必须明确：

1. 研究对象、目标人群和生物学或患者层面的统计单位；
2. 暴露、比较、分组、主要结局和必要协变量；
3. 发现、验证、敏感性分析和探索性分析的边界；
4. 数据能够支持的证据层级与 claim ceiling；
5. 能否推翻或实质削弱中心结论的结果；
6. 已知混杂、数据重叠、泄漏、伪重复和过拟合风险。

代码可运行、图件显著或样本量大，都不能替代有效的研究设计。

## 5. 活动路线的五个硬门槛

路线只有同时满足以下条件，才能从候选进入 `approved` 或 `active`：

1. `scientific_validity=pass`；
2. 必需数据已有最小样本或文件验证，关键 metadata 可取得；
3. 每个必需分析模块已有 A/B 级代码来源或经过同等测试的重建代码；
4. 最低充分的正文图已映射到数据、分析模块、代码、source table 和验收测试；
5. 发表重合不是 `duplicate`，并有完成的人工批准记录。

任何一个门槛失败，都应修复、降级为训练路线、保留为备选或停止；不能靠增加无关分析掩盖。

## 6. 数据可用性的含义

“论文给出 accession”或“网页能打开”不等于数据可用。批准前至少要记录并验证：

- accession、下载地址、访问类型和最后核验日期；
- 平台、疾病、队列规模和真实生物学单位；
- 表达层级、原始或处理后文件、文件大小和校验和；
- 路线需要的样本 ID、患者 ID、分组、结局、批次、治疗等字段；
- 数据集之间的患者、样本或来源论文重合；
- 用于 discovery、validation、spatial validation 或 training 的具体角色；
- 最小样本下载或完整下载是否真实成功。

缺少中心分析不可替代的字段时，数据门槛不通过；允许更换数据或降低主张，不允许臆造字段。

## 7. 代码可靠性的含义

找到 GitHub 链接不等于代码可用。每个代码来源按下列标准分级：

- `A`：版本固定、许可证明确、环境和入口明确、非交互运行、无硬编码路径和私有依赖，
  安装成功且完整测试通过；
- `B`：核心模块可非交互运行，环境和入口明确，安装与 smoke test 通过，只需小幅适配；
- `C`：只能提供局部函数或处理细节，需要较多重建；
- `D`：无法安装、依赖私有输入、许可证或入口阻断，不能作为执行 donor；
- `U`：尚未核验。

活动路线的必需模块只能依赖 A/B 级来源。C 级可以用于 P2 路线的重建参考，D/U 级不能被
表述为“已有可靠代码”。由 AI 补写的代码必须进入 `reconstructed` 来源，经过安装、smoke、
单元或科学不变量测试后，才能升为 A/B。

## 8. 发表重合检查

重合检查分别比较疾病、核心对象、结局、数据集、分析骨架、图件叙事和中心主张，并记录
检索式、近邻论文、核验日期和可区分点。

- `clear`：未找到实质相同研究；
- `adjacent`：有近邻，但目标稿件可明确区分；
- `high_overlap_distinguishable`：高度相似，必须在题目、主张或证据中明确区分；
- `duplicate`：中心主张和主要证据实质重复，必须停止该路线。

重合应在路线筛选时检查一次，并在投稿前按最新文献再检查一次。

## 9. 透明优先级，不使用不透明总分

Paper2Paper 按代码、数据、图件闭环、科学性、锚点复用、初学者负担和发表重合的固定顺序
比较路线。`P0` 至 `P3` 由公开规则推导，不能由人工随意填写，也不把多个维度压缩为加权总分。

当多个路线都合格时，优先选择能最大复用可靠代码和已验证数据、能较快形成完整图件、
且初学者容易审查的路线。用户保留最终选择权。

## 10. 结果可以改变计划

结果可产生 `continue`、`refine`、`reroute` 或 `stop`。任何 `refine`、`reroute`、`stop`
必须先保存原计划和原结果，再建立 Change Request、计算下游影响并完成所需人工审查。

不得为了得到阳性结果反复更换阈值、分组、终点、队列、marker 或 signature。看过结果后产生的
新分析默认是探索性的，除非获得独立验证。

## 11. 稿件导向的范围控制

新增工作必须服务于稿件 claim、图件、数据或代码资格审查、发表重合、正确性、最低复现要求
或人工审查。每个 WorkItem 必须有最低交付物、稿件去向和停止条件。

达到“可生成、可复查、可追溯”的最低充分实现后，不为通用化、优雅化或自动化本身继续扩展。

## 12. 人机分工

AI 可以采集信息、建立路线组合、生成和修复代码、执行测试、整理差异和计算下游影响。
AI 的路线和高影响决定默认是 `proposed`。用户负责批准活动路线、不可逆的数据取舍、主张边界
和最终稿件定位。重要决定必须进入 registry 和 review，不能只留在对话上下文。

## 13. 完成定义

Paper2Paper 项目的完成以稿件交付衡量：

- 活动路线通过五个硬门槛；
- 数据、代码、参数、环境和运行可追溯；
- 正文和补充图有 source table 与验收记录；
- 结果足以支持中心主张，阴性与限制被如实呈现；
- 方法可由新环境重建；
- 投稿前发表重合检查已更新；
- 形成结构完整、结论不过度的论文稿件。
