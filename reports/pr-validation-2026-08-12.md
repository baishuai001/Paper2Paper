# 2026-08-12 提交前验证摘要

本报告只说明本分支准备提交到公开 GitHub 的范围和验证结果，不代替 CRC 科学审查报告，
也不把 Pilot 结果写成目标论文已经成立。

## 本地与服务器版本关系

同步前有 65 个文件只存在于本地，另有 5 个同路径不同内容的文件。逐文件比较后的处理是：

- `learning/findings.tsv`、`evaluate_crc_cnmf_stability.py`、
  `phase-zero-real-run-report.md` 和 `reports/workflow-learning.md` 采用本地较新的最终版；
- `run_crc_cnmf.py` 不能简单说成谁覆盖谁：五组正式 K=5–20 运行实际使用的快照为
  SHA256 `E8AC3E7558861F8EE51705EFDCFAE0B2773534CE9C73C48E4B6670572AA934B0`；
  当前可复用代码及两项预定敏感性运行使用增强版
  SHA256 `628AB5250B446E66F2BFC2E94CCF9D851F4BAD3F99D0E190ED8F620C96E9D5E7`；
- 正式运行快照、当前代码和两套回执分别保存，未把当前代码误写成当时运行代码；
- 65 个本地精简文件和 5 个确认后的本地最终版本已同步到服务器；服务器旧冲突版在同步前
  进入被 Git 忽略的备份目录。

最终同步白名单明确拒绝 `tmp/`、`WorkSpace/`、raw/private/controlled/downloads 等路径，
`runtime_raw_paths_uploaded=0`。服务器约 70 GB 的原始数据、NMF 分片、usage、spectra 和完整
中间结果仍只留在服务器；没有上传 GitHub，也没有下载到本地。

## GitHub 公开范围

允许进入提交：

- Paper2Paper 规则、文档、schema、校验代码和必要测试；
- HCC→CRC Pilot 的锚点/逐子图审计、代码、执行合同和人工审查报告；
- 精简运行回执、环境/代码哈希、程序级或样本级汇总、少量审查图和测试摘要。

明确排除：

- H5AD、原始/受控数据、完整 NMF 中间文件和服务器 `tmp/`；
- 逐细胞分数、逐细胞选择表、完整基因特征映射和大范围全样本清单；
- 本机 `HANDOFF.md`、凭据、主机信息和原始服务器日志目录。

完整 PR 相对 `main` 改动 344 个路径（含删除旧平台文件），其中新增/修改 308 个公开文件，
当前内容约 2.9 MB，最大单文件 143,514 bytes；没有超过 1 MiB 的文件，
也没有 H5AD、H5、loom、RDS/RData、zarr、FASTQ、BAM 或 CRAM。敏感信息扫描未发现密码、
服务器主机、GitHub token、私钥、本机绝对路径或赋值形式的 secret。46 个文件仍含实际服务器
账号目录片段，但全部限定在不可改写的运行回执、服务器绑定快照或产物收据中；它们不含主机
或密码，保留是为了不伪造实际执行来源。

## 验证结果

- 本地核心：38/38 通过（2.718 秒）；胃癌 NRRS Pilot 与 HCC→CRC Pilot 均通过结构和合同校验；
- 服务器核心：38/38 通过（0.702 秒）；两个 Pilot 均通过；
- 服务器冻结 Phase 0 Python 环境：61/61 通过（3.383 秒）；
- `git diff --check`：通过；
- 正式运行代码快照、敏感性代码和稳定性 evaluator 的 SHA256 与各自冻结清单逐项一致。

本机没有安装 Phase 0 所需的 `scipy/anndata`，因此本机科学测试只运行到依赖导入检查；没有
临时安装另一套未冻结环境来制造“本机通过”。同一代码已在实际服务器冻结环境完成 61/61。

## 仍然成立的边界

- 这些测试证明代码合同、错误关闭和已登记结果的一致性，不证明 CRC 目标论文主线成立；
- Che 外部复核没有确认预设的 P1、P7、P8 程序，P0-GATE 仍未通过；
- 不能因工程验收通过而自动进入富集、空间、临床或扰动分析，也不能自动晋升 manuscript project。
