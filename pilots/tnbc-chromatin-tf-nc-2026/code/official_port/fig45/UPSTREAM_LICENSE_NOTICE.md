# Upstream licence notice

The plotting blocks extracted by `materialize_official_blocks.py` originate
from CodeOcean capsule `7227095/v1`, commit `edf5314`. The capsule contains an
MIT licence (`code/LICENSE`, SHA-256
`470349c2333c9e595d08f10fab1bfb18a29c8c909a502baac93e12b1fe13488c`).
An exact copy is retained as `UPSTREAM_LICENSE.txt` in this directory.

Generated `exact/` and `patched/` files retain their source-path and line-range
provenance in `materialization_receipt.json`; their unified diffs are stored in
`diff/`.
